services:
  - type: web
    name: nfl-win-probability
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python3 -m uvicorn nfl_ml.web:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
